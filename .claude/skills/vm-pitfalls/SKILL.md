---
name: vm-pitfalls
description: Known pitfalls (落とし穴) collected while building Arrow's resolver + bytecode VM. Read this BEFORE benchmarking or A/B-measuring an interpreter change, adding/changing a VM opcode or compiler eligibility rule, trusting that a gate/verification script is green, or writing a PowerShell script that spawns a child process or bulk-rewrites source files. Every entry is a mistake that was actually made — most of them passed all tests while being wrong.
---

# 落とし穴（既知） — 解決層 / バイトコード VM

Phase R（AST 解決層）・Phase V（バイトコード VM）の実装で**実際に踏んだ**罠。
各項の `#N` は [IMPLEMENTATION_LOG.md](../../../IMPLEMENTATION_LOG.md) のタスク番号。
計画・現在地は [BYTECODE_VM_PLAN.md](../../../BYTECODE_VM_PLAN.md)。

> 共通する形は **「テストは通ってしまう」「ゲートは緑のまま」**。
> ここに並ぶのは *壊れ方が見えない* 失敗ばかりで、見えるバグはそもそも載っていない。

---

## 1. 計測と A/B ベンチ

- **⚠⚠ 実行命令の割合は「時間の割合」ではない**（#46）。`Jump` は実行命令の **12%** だが
  時間では **0.3%**（空ループで命令数 -20% にしても **1.003x**）。**命令には値段の差がある**。
  ⇒ 命令数の内訳は **当たりを付ける** のには使えるが、**速度の予測には使えない**。
  安い命令ではなく **`exec_op` 1 回あたりのコスト** か **高い命令**（`Call`/`GetAttr`/`Store*`）を狙う。
- **⚠⚠ 最適化は「何個あるか」ではなく「何回動くか」で決める**（#24）。逆アセンブルの静的頻度で
  1 位だった `POP; RETURN_NIL`（6.68%）は **最上位文ごとに 1 回** 動くだけで、1.5 億命令の
  ベンチには現れない。**到達不能コードの除去は原理的に速度へ効かない**（実行されないものを消すため）。
  ⇒ 判断の前に **ディスパッチループで実行 op を数える**（一時計測でよい。#24 では入れて測って撤去）。
- **⚠ ノイズ床のプローブは「変更と同規模」で測る**（#27）。opcode を足す規模の摂動は
  **1 命令も実行しなくても** ベンチを 0.88〜0.94x 動かす（小関数 1 本のプローブは規模不足）。
  ⇒ この規模の変更は VM 支配ベンチを ±5% 揺らす。**数 % で良し悪しを決めない**。
- **⚠⚠ 「保守性の変更だから速度は動かない」は成立しない — ホットパスに `Vec` を 1 個置くだけで 0.90x**（#76）。
  4 経路のマーシャリングを畳んだとき、共有関数へ渡す小さな表を `.collect()` で作ったところ
  **FFI 呼び出しごとに 1 回ヒープ確保**が増え、`cdll_v3_add` が **0.900x**（2 メトリクスで一貫）。
  `std::array::from_fn` の固定長配列に変えて解消。⇒ **呼び出しごとに通る経路に `.collect()` /
  `Vec::new` / `to_string` を新設しない**。上限が決まっているなら固定長配列にする。
- **⚠⚠ ノイズ帯は固定値ではない。負の対照は A/B と同じセッションで取る**（#83）。
  同じ `cdll_v3_add` の同一バイナリ A/B が、時間帯によって **±1%** だったり **±3.5%** だったりした。
  ⇒ **前のタスクで測った帯を使い回さない。** `#[inline]` 版を退行と判定できたのは、
  **同じ時間帯に取った ±1% の対照**と並べたから。
- **⚠⚠ 「触っていない経路のベンチ」が動いたら、それは配置の話**（#83・#28/#62 に続く 3 度目）。
  `ar_call_fn` を切り出したら **`ar_call_fn` を 1 度も通らない** cdll ベンチが 0.94x に落ち、
  **`#[inline]` → `#[inline(always)]` に変えるだけで 1.04x に振れた**。
  ⇒ **まず「その経路をそのコードが通るか」を確かめる**。通らないなら速度議論ではなく配置議論。
  ⇒ 呼び出し元が 1 箇所しかない関数を切り出すときは **`#[inline(always)]` で生成コードを保つ**
  （#10-b の「重い本体をアームに書くな」とは別件 — 呼び出し元が 1 つならコードは複製されない）。
- **⚠⚠ 「ノイズか実差か」は必ず負の対照（同一 exe 同士の A/B）で決める**（#76）。
  `cdll_v3_add` は**同じバイナリ同士でも 0.966〜1.031x** 振れる（＝ノイズ帯 ±3.5%）。
  この帯を知らずに 0.966x を見ると「退行」と読んでしまう。逆に **0.900x が 2 メトリクスで
  一貫して出た**ときは帯の外なので実差と判定できた。⇒ **帯を測ってから値を読む**。
- **A/B は必ず交互実行**（A,B,A,B…）。A を N 回 → B を N 回だとサーマルドリフトで
  実在しない退行が見える。
- **⚠⚠ 「exec が N 倍」は「全体が N 倍」ではない**（#50）。例題 1 本の中央値では
  **exec は全体の 14%**（0.46ms / 3.40ms）しかなく、**exec を無限に速くしても端点は 1.59x**。
  残りはプロセス費用 1.5ms・`interp_init` 0.32・`type_check` 0.18・`parse` 0.17。
  ⇒ 速度の話をする前に **[prof_dist.ps1](../../../prof_dist.ps1) で段別分布を取る**。
- **⚠⚠ 1 回目の実行はコールドリードを踏む — プロファイラだけでなく素のベンチもである**（#50 → #70）。
  #50 では `startup` が **0.1ms → 10ms** に化け、最初の集計が丸ごと汚染された。
  #70 では **`cargo build` 直後の初回**の `bottleneck_bench.ar` が `fn call` を **0.405µs** と出し
  （2 回目以降は **0.123〜0.135µs**＝記録どおり）、**存在しない 3x の退行を報告してしまった**。
  ⇒ **必ず 2 回以上走らせて 1 本目を捨てる**。⚠ 「前回測定から時間が空いた値」との比較でも
  同じ誤りが起きる（#21-b）。**退行を主張する前に、同じセッションで A/B を取り直すこと。**
- **⚠⚠ プロファイラの「もっともらしい分布」ほど負の対照で裏を取る**（#50）。VM フレームが
  返った後にサンプラーの「今の op」を戻し忘れ、**`event_handler.ar` の 1 秒の待ちが丸ごと
  `ReturnNil` に化けて「`ReturnNil` が 72%」という一見それらしい結果**が出ていた
  （実際は `CallMethod` が 99.95%）。帰属は**独立な解析値**と突き合わせること
  （#50 では 500 万回の呼び出しベンチで `call − empty` の差分と照合した）。
- **⚠ op ごとに時計を読む方式でプロファイルしない**（#50）。安い op ほど相対誤差が大きくなり、
  「命令には値段の差がある」という**肝心の量が歪む**。**別スレッドからの統計サンプリング**なら
  サンプル数がそのまま滞在時間に比例する（計測ビルドの下駄は実測 0.95x ＝ 系統誤差なし）。
- **A/B は当該変更だけを切り替えて取る**（#21-b）。前回測定から時間が空いた値と比べると
  他の変更やマシン変動を誤って帰属する（`bench_field_access` の退行を #21-b のせいと誤認した実例）。
- **退行を疑ったら「そもそも触っているか」を先に見る**。`--vm=off` での切り分けは #33 で失われたので、
  **同じ 2 バイナリで測り直して再現するか** と「変更と同規模のプローブ」で判断する。
- **⚠ null 結果こそ「そもそも触っているか」を先に確かめる**（#46 では 2 バイナリの逆アセンブルを
  突き合わせ、変更が効いている経路を測っていることを確認してから捨てた）。
- **HEAD のバイナリが要る A/B・IR 比較では `git stash push -- src/` で退避** してビルドする。
  実行前に必ず `git status` を確認し、`src/` をスクラッチパッドへコピーしておく
  （過去に未コミット変更の破棄事故あり）。
- **⚠ `#[inline]` は効いているとは限らない**（#1-x）。巨大関数は LLVM が却下する。

- **⚠⚠ IR ダンプ・`--compile` の直後にベンチを取らない**（#64）。
  [dump_native_ir.ps1](../../../dump_native_ir.ps1) は **clang を 6 回起動して `.arc`/`.ars` を
  書き換え・復元する**。その直後に測ると値が汚れ、#64 では**同じ 2 バイナリが
  `partial_call_overhead.ar` で 0.897x と 1.007x** を出した（同規模プローブは 1.018x で再現せず）。
  ⚠⚠ **負の対照（同一 exe の A vs A）は「計測系は安定」の証明にならない** — `ab_bench` は
  交互実行なので**時間相関のドリフトを打ち消してしまう**。
  ⇒ 疑わしい差は **3 者（HEAD / プローブ / 変更後）を同一セッションで交互に**測って決める。
  ⚠ `partial_call_overhead.ar` は #69 でも 0.887x の外れ値を出した。**数 % の判定に使えない**。

- **⚠⚠ `flat_bench.ar` は配置ノイズだけで ±7% 動く**（#62）。`Cargo.toml` に `[profile.release]` が
  無い ＝ **`codegen-units = 16`（既定）・LTO 無し**なので、**コードをモジュール間で動かすだけで
  CGU 分配・インライン化・配置が crate 全体で変わる**。#62 では
  **`mod` 宣言の順序を逆にしただけ**のプローブが、リファクタリング本体と同じ **0.929x** を再現した
  （`bottleneck_bench.ar` は 0.969x で**完全一致**）。
  ⇒ **このベンチでリファクタリングの良し悪しを判断してはいけない。**
  ⚠ **「再現したから実差」は誤り**。再現性は必要条件でしかなく、**同規模プローブとの比較**が要る。
  ⚠ 敏感な理由は「25M 回の native↔解釈コールバック」という**境界支配**の形だから。
  ⇒ 疑わしい差が出たら **①同一 exe の負の対照 ②区間別の内訳 ③分散 ④同規模プローブ** の順に潰す。

## 2. VM / コンパイラを触るとき

- **`exec_op` は `#[inline(always)]`。op のアームに重い本体を書かない**（#10-b）。その op を
  使わない Chunk のホットループまで 4〜6% 遅くなる。逆に **全部外へ出すと** その op を使う側が
  7〜10% 損する。→ **IC はヒット経路だけインライン・ミス経路は `#[inline(never)]`**。
  （#48 で同じ罠を再演し 0.91x の退行を出した ＝ **判定を `Op::Call` のアームに直接書いた**。）
- **診断フックを実行経路に足すときは feature で消せる形にする**（#10-a）。`exec()` は文ごとに
  呼ばれるので `OnceLock` の atomic 読み 1 回でも **11% 退行** した
  （`cfg!(feature=..)` を先に見て定数 false にする）。
- **⚠⚠ 採番のずれは #86 以降デバッグビルドが止める。ただし「止まらなかった＝正しい」ではない**。
  網は 2 本で、**壊れ方が 2 通りある**から: ①**別の変数を読む**（`Compiler::local_slot` が
  コンパイル時にリゾルバの `Resolution::Local` と `slots` を突き合わせる）②**範囲外を読む**
  （`into_chunk` の `verify_storage_indices` が全命令の記憶域索引を検査）。
  ⚠ **どちらも `debug_assert` なので release では消える** — ゲートを `target/release` で
  回していると**この網は 1 度も動かない**。⇒ 採番に触ったら**デバッグビルドで例題を回す**。
- **⚠⚠ assert を書く前に「誤検知しないか」を実装より先に確かめる**（#86）。2 つ踏みかけた:
  ① `for` ターゲットのシャドウは**本体の間だけ `slots` を temp slot へ差し替える**ので、
  素朴に突き合わせると誤検知する（→ リゾルバが `collect_shadowing_binders` の名前を
  `base` から外していると確認できたので衝突しない）。
  ② `local_names.len() == n_locals` は**不変条件ではない**（`alloc_temp` が `n_locals` だけ
  伸ばし、`local_names` は名前付き slot だけ）。**等号で書いていたら全例題で誤検知していた。**
  ⇒ **「たぶん成り立つ」で assert を書かない。成り立つ根拠をコードで確かめてから書く。**
- **⚠⚠ 発火しない assert は無意味 — 「歴史的な失敗そのもの」を再現して検出力を測る**（#86）。
  `collect_base_decls` から `Stmt::Static` を落とす（#27 の実バグの再現）と
  `'acc' をリゾルバは slot 2、コンパイラは slot Some(3)` で**発火**した。
  ⚠⚠ **同じプローブを release で走らせると `TypeError: ... int and NoneType`** ＝
  **原因から遠い場所で、原因を示さないメッセージ**になる。
  ⇒ この種の網の価値は「落ちること」ではなく「**落ちる場所と理由が原因と一致すること**」。
- **⚠ 記憶域が 2 種類あるなら索引も分けて検査する**（#86）。ローカル slot は `n_locals`、
  **セル索引は `n_cells`** で縛られる別の記憶域。1 つの上限でまとめて検査すると
  **`LoadCell` が別の値を読む形を見逃す**。
- **⚠ コード索引を持つ op は `peephole::code_target_mut` に登録する**（#27-d）。
  `ForIter` の exit・`SetupTry` の handler・`StaticInit` の after。
  ⇒ **#87 で exhaustive 化した**ので、**op を足すとコンパイルが止まる**ようになった
  （`Op` +1 で止まる箇所は #86 の 3 → **4**）。⚠ **`_` を書き足して無効化しないこと。**
- **⚠⚠ exhaustive 化が守るのは「新しく足す」側だけ — 「既存の登録を外す」側は別の網が要る**（#87）。
  実測したら **`StaticInit` を `code_target_mut` から外しても 750 テストが全部通った**
  （＝ #27-d の実バグがそのまま再演できる状態だった）。`ForIter` / `SetupTry` は
  単体テストが FAIL したのに、`StaticInit` だけ網が無かった。
  ⇒ **登録した op は 1 つずつ「外したら落ちるか」を実測する**。落ちないならテストを足す。
- **⚠ doc からの機械抽出は「否定表現」を読めない**（#87）。「飛び先を持つ op」を doc の
  キーワードで拾ったら **`Op::Fail` が偽陽性**で挙がった — その doc は
  「⚠ **飛び先索引を持たない**ので `code_target_mut` は不要」と**持たないことを明記**している。
  ⇒ 候補は「**人間が 1 件ずつ判断するための入力**」であって答えではない。
  分類表には「**言及しているが持たない**」の受け皿と理由を置く。
- **⚠⚠ 採番はリゾルバと同順・同数**。`push_base` される名前は `slots` に入れなくても
  **必ず 1 slot 消費する**（`static` で飛ばして `LoadLocal` が範囲外を読んだ）。
- **同じ木を歩く walker が 2 つあるとずれる**。`compile_stmt` に文種別を足したら
  **必ず採番側（`collect_nested_decls`）も見る**（#27-c で 2 回踏んだ）。
- **⚠ `*_evaled` 版とずれた実装を作らない** — 実バグ 4 回。同じ判断をする 2 実装は
  片方を委譲に畳む。畳めないものは **不変条件をテストで固定** する。
  ⚠ #48 の実バグはこの形: **VM は評価済みの値で呼ぶ ⇒ `dispatch_native_evaled` に落ちる ⇒
  この経路は設計として書き戻しをしない**（引数の AST 式が無いと書き戻し先が決まらない）ため、
  `import[cpp-lib]` のプリミティブ out 引数（`double*`）が **黙って 0.0 を返していた**。
- **⚠⚠ `Vec<T>` を `Rc<[T]>` に変えると `.clone()` の意味が黙って変わる**（#45）。
  「中身の複製」から「**参照カウント加算**」になり、**コンパイルは通る**。スレッド送出経路
  （`deep_clone`）でこれをやると **#15 の穴（非アトミック参照カウント）が復活** する。
  該当は 5 箇所中 3 箇所。**型では守れないのでポインタ同一性を見るテストで固定** する。
  ⚠ **async の実地ストレスでは検出できなかった**（競合窓が狭い）。**決定的な検査を別に置く**。
- **⚠⚠ 最上位 Chunk キャッシュは `Stmt` の *アドレス* をキーにする**（#36）。載せた AST を解放すると
  アロケータが同じアドレスを再利用し、**別の文が前の文の Chunk を実行する**（REPL がブロックごとに
  AST を捨て、`let xs = …` が `let total = …` の Chunk を走らせた）。
  新しい入口は **AST を保持し続ける**。
- **⚠ 脱出制御を足すときは「オペランドスタック」と「ハンドラスタック」の両方を戻す**（#34）。
  `has_escape` は **文しか歩かない** のでブロック式の中の `break` を見落とす。`try` から跳ぶと
  `PopTry` を通らず **ハンドラが残り、ループを抜けた後の無関係な例外を横取りする**。
  検知には「**跳んだ後に別の例外を投げる**」例題が要る（跳ぶだけの例題では素通りする）。
- **⚠ 必ず失敗する文は bail せず `Op::Fail` で同じ文言を出す**（#34）。実行時エラーになると
  分かっている文（囲むループの無い `break`）を bail すると `VmForceError` になり、
  **本来のエラー文言が消える**。
- **⚠ VM は「解決情報が揃っている」前提**（#3/#36）。供給しない入口では、正しいコードでも
  `VmForceError` になる。⇒ **#88 で配線を 1 箇所へ畳んだ**
  （[`resolve_and_annotate`](../../../src/interpreter/resolver.rs) が AST 側、
  `Interpreter::wire_resolution` が注入側。**3 つの setter は private** なので
  **注釈だけ渡してグローバル集合を忘れる形はコンパイルが通らない**）。
  ⚠ 例題は全部 `run_program` を通るので **例題だけ見ていても入口の穴に気づけない**。
- **⚠⚠ 「入口ごとの手写し」は、測ると必ず理由の無い差が混ざっている**（#88）。5 入口を表にしたら
  **①型検査と解決の順序が逆の入口があった**（型検査が `Resolution` を読まないので**当時は無害**
  ＝ だから誰も気づかない）**②型検査の入口が 2 種で片方は警告を落としただけの逐語コピー**。
  ⚠ 一方で **畳んではいけない差も同数あった**（型エラーの扱い 4 通り／REPL だけ `extend_`／
  `--compile` は `Interpreter` を持たない／モジュール本体は**意図的に供給しない**）。
  ⇒ #72 と同じ結論: **「手写しだから畳む」ではなく、1 つずつ理由を確かめてから畳む。**
- **⚠⚠ 配線を 1 本にしても「片方だけ呼ぶ」は塞げない — 可視性で塞ぐ**（#88）。
  対で要るもの（注釈と最上位グローバル集合）は **1 つの関数で受け取り、個別 setter は private**
  にする。負の対照は「入口から直接呼ぶプローブを書いてコンパイルが止まるか」。
- **⚠ 段別プロファイルのタイマーを共有関数へ移したら、段が潰れていないか実測する**（#88）。
  `Phase::TypeCheck` / `Phase::Resolve` を `resolve_and_annotate` の中へ移したので、
  [prof_dist.ps1](../../../prof_dist.ps1) が依存する内訳が消えうる。⇒ `AR_PROF=1` で
  **2 段が別々に出ること**を確認した。
- **⚠ worker スレッドは `Interpreter::new()` を作る**（#32）。**型注釈等を引き継がない** ので、
  親の設定を渡し忘れると **ゲートに穴が開く**（実際に開いていた）。
- **`Rc` を含む値をスレッドへ送る経路（async）では `deep_clone` が独立バッファを作ること**（#15）。
  回帰検知は [async_string_share.ar](../../../examples/async/async_string_share.ar)
  （**接触回数を減らすと検知力を失う**）。
- **注釈（`res`・型）は最適化ヒントであって意味論の根拠ではない**（#15e）。破ると
  「同じコードが書かれた場所で挙動を変える」（**実バグ 5 件**）。
  ⚠ `Resolution::Unresolved` は「シャドウが無い」を意味しない。判定は **実際の束縛／出自** を見る。

## 3. 検査網・ゲートの信用（**最も繰り返し踏んだ類型**）

- **⚠⚠ 検査網は「例題が踏む形」しか見ない**（#27/#34/#36/#33/#41 で **5 回** 踏んだ）。
  `force_gate` 0 件・`tw_control_flow` 0 は **例題が必ずその形を書いている前提** に乗っている。
  新しい判断をする前に、**その形の例題があるかを先に確かめる**。
- **⚠ `force_gate` 0 件は「その時の例題で 0」であって「言語全体で 0」ではない**（#34/#35）。
  **例題が無い言語機能はゲートに映らない**。単体テストだけが押さえている形が現に 11 件あり、
  そのうち 5 件は `VmForceError` で止まっていた。**新しい形を疑ったら例題を先に書く**。
- **⚠⚠ ゲートは「緑だと報告された」ではなく「自分で走らせて緑」を確かめる**（#30 発見・#44 修正）。
  `debug_session.ps1` は `6bf039c` から 5/5 赤いままで、しかも完了報告には「5 identical」と
  書かれていた。**修正とその golden を同じコミットで入れると、録り直し漏れがレビューでも
  気づかれない**。
  ⚠ さらに **「緑だった環境と同じとは限らない」**（#49）— 2 度目の赤は src も golden も無関係で、
  **コンソールのコードページ依存** だった（別マシンでは緑のまま通る）。
- **⚠⚠ 赤いゲートは「検知力が落ちる」のではなく「別物を検査する」**（#44）。`step_over_heavy` は
  BOM がコマンドを 1 つ食っていたせいで、**名前どおりの step-over に到達する前に終わっていた**
  （＝ずっと「step-over を検査しているつもり」だった）。
  ⇒ golden を録り直すときは **`-Update` の前に「違う」ではなく「正しい」ことを 1 値ずつ裏取りする**。
- **⚠ ゲートが「例題の書き方」に依存していないか疑う**（#36）。`toplevel_vm_candidate` が
  `!toplevel_globals.is_empty()` を条件にしていたため、**最上位に宣言の無いプログラム** は
  最上位が丸ごとツリーウォークだったのに `force_gate` 0 件のままだった。
  **負の対照が発火しないときは、まず配線を疑う**。
- **⚠ 既存ゲートは exit code しか見ない**（#47/#48）。FFI が **黙って間違った値** を返しても全部緑。
  ⇒ 値を検査する例題は **期待値と違えば `raise` する** 形で書き、
  **修正前バイナリで exit=1 になること（負の対照）を確認** する
  （[cpp_out_param_writeback.ar](../../../examples/interop/cpp_out_param_writeback.ar)）。
- **⚠ `force_gate` は例題ごとに最初の 1 件で止まる**（1 例題 = 1 原因ではない）。潰すたび測り直す。
  文言だけで bail と `vm_ineligible` は区別できないので `AR_TW_STATS` の両表を突き合わせる。
- **bail する形はツリーウォークが正しいとは限らない**（`for-target-shadow` で実バグ）。
  基準は `python -m impl_python` の出力（[compare_python_impl.ps1](../../../compare_python_impl.ps1)）。
- **ゲートスクリプトは `target/release` を見る**（`cargo build` の debug だけ見て「直った」と
  判断しない）。**`$ErrorActionPreference='Stop'` から `cargo` を呼ぶと進捗の stderr で終了
  エラーになる** ので、その呼び出しの間だけ `Continue` に落とす（`tw_stats.ps1` 参照）。

- **⚠⚠ ソースを機械計測するときは文字リテラルも潰す**（#60）。#58〜#68 の診断は
  行コメントと**文字列**リテラルだけを除去してブレース平衡を取っていたため、
  `'{'` / `'}'` を大量に持つ **C/C++ ヘッダのパーサ 2 本**で関数の終わりが**ファイル末尾まで伸び**、
  `parse_struct_bodies` を **307 行（実際は 103 行）**、`scan_scope` を **405 行（実際は 82 行）**
  と報告していた。⚠ **測り直しで自分も同じ罠を踏んだ**（最初のスクリプトが例外を投げた）。
  ⚠ ネスト深さのほうは正しかった（インデントで測るため平衡に依存しない）。
- **⚠⚠ 「実ファイルを食わせる」テストほど中身が空になりやすい**（#60）。
  `test_real_dxlib_header_structs` は実ヘッダ（2,445 宣言・65 struct）を読みながら
  **「VECTOR という名前が居る」だけ**を見ており、パーサを丸ごと書き換えても素通りした。
  ⇒ **件数・レイアウトを固定する**こと。
  ⚠ ただし**実ファイル検査だけでも足りない** — #60 の負の対照（`trim_start` → `trim`）は
  実ヘッダ検査を**通り抜け**、合成テスト `test_union_incomplete` だけが検知した。
  **広い回帰は実ファイル、意味論は合成テスト**の役割分担が要る。

- **⚠⚠ 例題スイートのグロブは非再帰のものがある**（#74）。
  [compare_outputs.ps1](../../../compare_outputs.ps1) と
  [compare_python_impl.ps1](../../../compare_python_impl.ps1) は `examples/<cat>/*.ar` なので
  **サブディレクトリの例題を 1 本も見ない**（`scan_examples` / `force_gate` は再帰する）。
  ⇒ サブディレクトリに例題を置いたら、**どのゲートが実際に拾うかを確かめる**こと。
  #74 では [compare_import_paths.ps1](../../../compare_import_paths.ps1) へ明示登録した
  （ついでに `import_py_search_path.ar` 等が**どのゲートにも入っていなかった**ことも判明した）。

- **⚠⚠ 「clippy 増分 0」は緑の証明ではない**（#65）。受け入れ済みの baseline
  （当時 51 件）の中に **`replacing text with itself`** があり、それは
  `ljust` の**実バグの行そのもの**を指していた（`replacen(&fill, &fill, width)`）。
  #58 以降ずっと「51/64・増分 0」を緑としてきたが、その 51 に混じっていた。
  ⇒ **baseline を一度は 1 件ずつ読む**こと。特に `replacing text with itself` /
  `parameter is only used in recursion` のような「意味が壊れている」系は要確認。

- **⚠⚠ この環境の heredoc は「クォート付き」でもバックスラッシュを 1 段食う**（#78・複雑さ診断でも同じ罠）。
  `cat > x.py <<'EOF'` に Rust の `'\\'` を書くと**ファイルには `'\'` が入り**、
  `unterminated character literal` で落ちる。⇒ **バックスラッシュを含むコードを heredoc で生成しない**
  （`chr(92)` で組み立てるか、書き込んだ後に置換で直す）。
- **⚠⚠ 大量サイトの書き換えは手写ししない — 機械変換して assert で守る**（#80）。
  `ClassValue` 11 サイト × 19 フィールドを機械変換したところ、**スクリプト自身の欠陥を
  assert が 2 回捕まえた**: ①フィールド短縮記法（`methods,`）を未対応
  ②`TemplateClassValue {` を `ClassValue {` として**誤マッチ**（単語境界が無い）。
  手で書いていたら**静かに壊れていた**。⇒ 変換は「切り出す → 判定 → 組み直す」を機械にやらせ、
  各段で `assert count == n` を置く。
- **⚠⚠ 「挙動不変」は触った場所によって見るゲートが違う。`compare_outputs` では見えない壊れ方がある**（#80）。
  `ClassValue` の構造体更新記法（`..synthetic(name, id)`）は **`alloc_class_id()` の評価を
  後ろへ動かす**。`class_id` は**インスタンスヘッダとネイティブ dispatch の GEP に焼かれる**ので、
  採番がずれても stdout は同じまま壊れうる。⇒ **`class_id` を引数で受けて呼び出し側に順序を残し**、
  [dump_native_ir.ps1](../../../dump_native_ir.ps1) の **IR byte-identical** で確認した。
  ⚠ 「codegen を触ったら IR が主検査」は、**codegen を触っていなくても**効くことがある。

- **⚠⚠ `rindex` で「末尾まで」を取る置換をしない**（#86 で自分が #78/#80 の教訓を破った）。
  `match` の中身を差し替えるのに `s.rindex(chr(10)+'}'+chr(10))` で末尾を取ったところ、
  **その後ろにあった `#[cfg(test)] mod mode_tests`（#52 の「唯一の防波堤」）ごと消していた**。
  ⚠⚠ **ビルドも clippy も例題も全部通った**。気づいたのは **`cargo test` が 750 → 748 に
  減った**ことだけ。⇒ **置換は「何を消すか」を assert できる形に切る**
  （アンカーを前後 2 つ取る／`assert old.strip().startswith(...)` を書く）。
  ⇒ **リファクタ後は必ずテスト件数を前後で比べる**（合否だけ見ていると減っても気づけない）。
- **⚠⚠ op を分類する表は「名前」で作らない — doc で作る**（#86）。`storage_operands` の
  初版は **op 名に `local`/`slot` を含むものだけ**を拾い、`IntBinLL` / `IntBinLC` /
  `FloatBinLL` / `FloatBinLC` / `UnpackTuple` の **5 つを「slot を持たない」と分類**していた
  （`LL` = `local[a] <op> local[b]`）。⚠ **exhaustive な match でも防げない** —
  網羅はしているが**分類が間違っている**から。
  ⇒ 生成器に「**doc に `local[` と書いてあるのに一覧に無い op があれば落ちる**」assert を入れた。
  **人間の記憶ではなく `op.rs` の記述を根拠にする。**
- **⚠⚠ 置換スクリプトには必ず件数の assert を入れる**（#78）。`src/lexer/math.rs` は **CRLF** で、
  LF 前提のパターンが**黙って 0 件マッチ**になり、負の対照が「適用されたつもり」で通りかけた。
  ⇒ `assert s.count(old) == 1` を必ず書く。改行コードは LF / CRLF の両方を試す。
- **⚠ タイムアウトを持つゲートを並走させない**（#78）。[scan_examples.ps1](../../../scan_examples.ps1) を
  比較ゲートと同時に走らせたら `partial_call_overhead.ar` が TIMEOUT した（単独ではクリーン）。
- **⚠⚠ 書き換える前に golden を取る**（#78）。`render_math_str` は分岐が 2×2×2 あるのに
  既存例題は 6 行しか踏んでいなかった。**HEAD のバイナリで 23 分岐の出力を先に保存**してから
  書き換え、byte-identical を確認した。⇒ **「網が無い所を触る」と分かったら、まず自分で網を作る**。
- **⚠⚠ 手で A/B するときは揮発値を正規化してから比較する**（#77）。
  例外系例題を `md5sum` で突き合わせたら 1 本が「差分」に見えたが、実体は
  `<ZeroDivisionError object at 0x1971969f8b0>` の**ヒープアドレス**だった。
  [compare_outputs.ps1](../../../compare_outputs.ps1) は `Normalize-Volatile` で
  `0x…` と 10 桁以上の整数（`id()`）を潰している。**手作業はこれを飛ばすので偽の差分が出る**。
  ⇒ 手 A/B は `sed -E 's/0x[0-9a-fA-F]+/0xADDR/g'` を通してから比較する。

- **⚠⚠ exhaustive 化の仕掛けが止めてくれるのは「2 段目」だけ**（#81）。
  `Expr` の再帰骨格を [expr_walk.rs](../../../src/expr_walk.rs) へ畳んだとき、
  **そもそも `match` アームのパターン式を列挙し忘れていた**（＝ #75 が入れた
  「`case <expr>` も参照として拾う」が静かに消えるところだった）。
  `SubPart::MatchPattern` を足した瞬間に**消費者 4 本すべてがコンパイルエラーで停止**したが、
  **「足すべきだ」と気づいたのは人間が読んだから**。
  ⇒ **1 段目（列挙の網羅）はコンパイラが保証しない。** 畳む前に「元の 4 実装が
  それぞれ何を見ていたか」を**表にして突き合わせる**こと。
- **⚠⚠ 「表にして突き合わせる」を実際にやると本当に実バグが出る**（#84）。#81 の助言に従って
  **8 walker × `Stmt` 40 variant の表を機械生成**したところ、「6 本は降りるが 1 本だけ降りない」
  というセルが 3 つ見つかり、**3 件とも実バグ**だった（`impl_python` は 3 件とも正しく動く）。
  ⚠⚠ **3 件とも例題が 0 本**（#56/#68/#71/#75 に続く **5 度目**）。裏取りは
  [compare_bytecode.ps1](../../../compare_bytecode.ps1) が **114/114 byte-identical** だったこと
  ＝ **網を広げても既存例題では 1 バイトも動かない**。
  ⇒ **目視で「たぶん揃っている」と言わない。表は機械で作る**（`match` のアーム名を grep するだけでよい）。
- **⚠ 「列挙し忘れ」に唯一気づかせてくれるのは `dead_code` 警告**（#84）。1 段目（列挙の網羅）は
  コンパイラが保証しないが、**種類を足したのに誰も payload を読んでいない**と `dead_code` が出る。
  #84 では `MatchPattern` / `ProtocolBody` が未読と出たことが「**7 本目の walker も変換すべき**」
  という判断につながった。⇒ **新しい種類を足したら `dead_code` を消す前に「読む walker が
  本当に無いのか」を確かめる**。本当に無いなら **`allow` は当該フィールド 1 つだけ**に絞る
  （#51 の「属性が別の警告を食う」を避けるため）。
- **⚠⚠ 「全ゲート緑」は「例題が書いている形については緑」でしかない — それを数える手段が
  #85 まで無かった**。実測すると、`Stmt` **39/40**・`Expr` **29/30** はカバーされている一方で、
  **「最上位 `fn` には書かれているのに入れ子 `fn`（クロージャ）の中には 1 度も書かれていない」構文が
  24 件**あった。⚠⚠ **歴史的な実バグ 5 件のうち 4 件（#68 / #75 / #84 ×2）がまさにこの文脈**。
  ⇒ 新しい構文・文脈を扱う前に [syntax_cov.ps1](../../../syntax_cov.ps1) を走らせ、
  **`NESTED-GAP` を見る**。「たぶん例題があるはず」で進めない。
- **⚠ カバレッジ計測は「実行」ではなく「パース」で取る**（#85）。実行すると GUI 例題の
  タイムアウト（`force_gate` は 161 本中 4 本がそれ）や async のスケジューリングで
  **値がマシンの都合で揺れる**。`AR_SYNTAX_COV=1` はパース直後に AST を歩いて**実行せず終了**する。
  ⚠ 「呼ばれない関数の中」も数えたいので、そもそも実行では答えが出ない。
- **⚠⚠ 診断ツールの検出力は「歴史的な実バグ」で確かめる**（#85）。#84 の例題を**一時的に外して**
  測り直したら `Static>Block` と `Match>Let` が**ちょうど 2 件**消えた ＝ #84 ①③ を検出できる。
  ⚠ 一方 **#84 ② は検出できなかった**（`Let>Block` は他の例題が埋めていた）。
  ⇒ **「検出できる範囲」まで測って書く**。「これで全部見つかる」と書かない。
- **⚠ 「測れなかった」を黙って 0 件に混ぜない**（#85）。カバレッジ集計で 7 本が測れず、
  理由を出したら **`interop/subdir/sub_*.ar` 2 本が実在しないモジュールを import したまま壊れていた**。
  ⚠⚠ **どのゲートも捕まえていなかった** — `scan_examples` は**非再帰**でそのディレクトリを見ず、
  `force_gate` は `VmForceError` しか見ないので `ParseError` は素通りする。
- **⚠ `dead_code` を消すために `allow` を外すなら、外して測ってから決める**（#85）。
  `stmt_walk::ModuleBody` は #85 で読む消費者ができたが、それは
  `#[cfg(feature = "tw_stats")]` の中なので**既定ビルドには居ない** — 外したら警告 2 件になった。
- **⚠⚠ clippy の基準値は文書を信用せず、同じセッションで HEAD を測り直す**（#84）。
  同じ時点について **プラン「50/63」・実装ログ「51/66」・実測「50/65」の 3 通り**が併存していた。
  ⇒ `git stash push -u -- src` して同じコマンドを打つ（1 分で済む）。**総数ではなく増分 0 を見る。**
- **⚠ 畳むときに現れる「片方だけが見ていたもの」は 3 通りに分類する**（#81/#76/#78）。
  ①**本当の取りこぼし**（広げるのが正しい: `collect_bound_in_expr` の `Slice`）
  ②**意図的な差**（残す: `collect_expr_decls` はパターンへ降りない＝採番がずれる）
  ③**潜在バグ**（現状観測できないので触らない: `render_math_str` の `unwrap_or(sym)`）。
  ⇒ **どれなのかを決めてからコメントに書く**。「なんとなく揃える」が一番危ない。

## 4. PowerShell / 子プロセス / エンコーディング

- **PowerShell 5.1 は BOM 無し `.ps1` を ANSI として読む**（日本語コメント入りは UTF-8
  **BOM 付き** 必須）。**Rust ソースの一括書き換えは Python で `encoding='utf-8'` を明示**
  （`Set-Content` は文字化けする）。
  ⚠ **そのスクリプトから日本語を `print` すると cp932 の `UnicodeEncodeError` で書き込み前に落ち、
  変更が丸ごと消える**。進捗表示は ASCII か件数だけにすること。
- **⚠⚠ 子プロセスへ stdin を流すときは `Process.StandardInput` を使わない**（#49）。
  .NET Framework の `Process.Start` は writer を `[Console]::InputEncoding` で作り
  `AutoFlush=true` を立てた時点で **preamble（BOM）を子へ書く** ⇒ **`Start()` が返った時点で
  もう入っている**。`.BaseStream` へ BOM 無しで書く手当ては **効かない**。
  手は 2 つ: **`cmd /c "exe < file"` のネイティブリダイレクト**
  （[repl_session.ps1](../../../repl_session.ps1)。stdout/stderr を分けられない）か、
  **起動直前だけ `[Console]::InputEncoding` を preamble 無しに差し替えて `finally` で戻す**
  （[debug_session.ps1](../../../debug_session.ps1)）。
  ⚠ **発火はコンソールのコードページ依存** なので、別のマシンでは緑のまま通る。
- **⚠ `ReadToEnd()` を stdout→stderr の順に逐次呼ぶと子とデッドロックする**（#34 で 1 時間停止・
  #38 で修正）。子が stderr のパイプ（既定 4KB）を埋めると書き込みでブロックし、親は stdout を
  待ち続ける。**必ず `ReadToEndAsync()` で同時に読む**
  （[scan_examples.ps1](../../../scan_examples.ps1) が手本）。
  症状は「**CPU 時間が伸びないまま生き続ける**」。
  ⚠ **普段の実行では踏まない**（子の stderr が数 KB を超えて初めて発火）ので、新しく子プロセスを
  起こすスクリプトを書いたら **`AR_VM_DUMP=1` を付けて回して確かめる**
  （`bench_branch.ar` で 7KB 出る ＝ そのまま負の対照になる・#38）。
- **⚠⚠ `ReadToEndAsync` でも足りない — 孫プロセスが生き残るとパイプが閉じない**（#58）。
  `import[js-proc]` は **node のブリッジを孫として起こし、それが arrow.exe より長生きする**。
  孫はパイプの**書き込み端を握ったまま**なので、`WaitForExit` が返った後の
  **`$task.Result` が永久に完了しない**（親は無限に待つ）。
  ⚠ **症状は §4 の逐次 `ReadToEnd` と見分けが付かない**（CPU 0 のまま生きている）が、
  **原因も直し方も別**。⇒ 見分け方は「**`arrow.exe` は既に消えているか**」
  （`Get-Process` に子が居らず node/dotnet だけ残っていれば孫の握りっぱなし）。
  手は **`Start-Process -RedirectStandardOutput/-RedirectStandardError` でファイルへ落として
  終了後に読む**（ファイルハンドルは孫が持っていても読み出しを妨げない・
  [compare_import_paths.ps1](../../../compare_import_paths.ps1)）。
  ⚠ [compare_bytecode.ps1](../../../compare_bytecode.ps1) が `js_proc_*` / `cs_proc_app` を
  skip リストに入れているのは**この理由**（当時は理由を書いていなかったので #58 で踏み直した）。
- **native exe の stderr を `2>&1` で受けると PS5.1 が ErrorRecord 化** して exit 0 でも失敗扱いに
  なる。`Start-Process -PassThru` の `ExitCode` も当てにならない
  （`System.Diagnostics.Process` を直接使う）。
- ⚠ `powershell -File` 経由だと `-Scripts a,b,c` が 1 要素に潰れる（`-Command` で呼ぶ）。

---

## 使い方（チェックリスト）

| これからやること | 先に読む節 |
|---|---|
| ベンチを取る／速度改善の可否を判断する | §1 |
| opcode・peephole・コンパイル適格性を触る | §2 |
| 「ゲートが緑だから大丈夫」と言おうとしている | §3 |
| 子プロセスを起こす／ソースを一括書き換えするスクリプトを書く | §4 |
