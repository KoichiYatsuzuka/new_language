class Foo:
    mut x: float
    fn __init__(mut self, x: float) -> None:
        self.x = x

mut pts: list[Foo] = [Foo(1.0), Foo(2.0)]
let fl = pts => fixed_list[Foo]
fl.append(Foo(3.0))   # TypeError: 'fixed_list' is immutable
