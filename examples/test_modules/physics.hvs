class Body->Body:
    mut x: float = ...
    mut y: float = ...
    mut z: float = ...
    mut vx: float = ...
    mut vy: float = ...
    mut vz: float = ...
    mut mass: float = ...
    fn potential(mut self, other: Body) -> float:
        ...
    fn kinetic(mut self) -> float:
        ...
    fn vel_dot(mut self, other: Body) -> float:
        ...
    fn dist_sq(mut self, other: Body) -> float:
        ...


fn total_energy(a: Body, b: Body, n: int) -> float:
    ...

fn total_energy_inline(a: Body, b: Body, n: int) -> float:
    ...
