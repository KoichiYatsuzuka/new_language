# py_multi_assign_error.ar — アンパック代入で**通さない**形（U2）。
#
# ⚠⚠ **この例題は 2026-09-19 に中身を絞った**。もとは「入れ子 `a, (b, c) = t`」も
#   踏ませていたが、**再帰で脱糖して通るようになった**（`py_multi_assign.ar` の ⑪〜⑭）。
#
# 期待されるエラー（ParseError・変換時）:
#   `*rest` must be the last target in unpacking
#
# ⚠ `a, *m, b = t` は `b` を**末尾から**数える必要がある（`m` の長さが実行時まで
#   決まらない）。末尾の `*rest` だけをスライスで受ける形に留める。
#
# ⚠ **通る形**は `py_multi_assign.ar`（複数代入 / 2・3 要素 / 末尾 `*rest` /
#   リスト形 / スワップ / ループ・while の中 / 添字ターゲット / 入れ子）。

import[py] test_modules.py_unpack_midstar_error as m

print("unreachable")
