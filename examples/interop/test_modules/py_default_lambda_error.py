"""py_default_lambda_error.py - 変換できないデフォルト式は明示エラーになる（項目 1）。

項目 1 の実装前はデフォルト値を丸ごと捨てていたので、この形は**黙って通って**いた
（そのかわり `h()` が引数不足で落ちた）。今はデフォルト式も変換対象なので、
lambda（項目 26 未実装）がここで表に出る。
"""


def h(k=lambda: 1):
    return k
