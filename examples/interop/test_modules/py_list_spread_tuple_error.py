"""py_list_spread_tuple_error.py — リストの `*` 展開にタプルを渡す（実行時 TypeError）。

⚠ これは**変換時ではなく実行時**のエラー。変換器は `[*t, 9]` を `t + [9]` に
落とすので、`t` がリストでなければ `+` が落ちる。Arrow に `list()` 組込みが無く
`list(t) + [9]` と書けないため。**黙って違う結果にならない**ことが要点。
"""


def g(t):
    return [*t, 9]
