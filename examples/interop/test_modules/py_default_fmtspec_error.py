"""py_default_fmtspec_error.py — 変換できないデフォルト式は明示エラーになる（項目 1）。

項目 1 の実装前はデフォルト値を**丸ごと捨てて**いたので、この形は黙って通っていた
（そのかわり呼ぶと引数不足で落ちた）。今はデフォルト式も変換対象なので、
中身が未対応ならその構文自身のエラーがここで表に出る。

⚠ 書式指定つき f-string は Arrow に相当構文が無い（FUTURE_FEATURE §4 (3)）。
"""


def g(n=1, msg=f"{n:.2f}"):
    return msg
