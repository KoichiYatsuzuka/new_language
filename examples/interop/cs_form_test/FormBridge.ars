class FormApp->FormApp:
    fn __init__(self: Self) -> None:
        ...
    fn message_box(title: str, message: str, buttons: int) -> int:
        ...
    fn input_box(title: str, prompt: str) -> int:
        ...
    fn show_note(title: str) -> int:
        ...
    fn get_note_title(handle: int) -> str:
        ...
    fn get_note_content(handle: int) -> str:
        ...
    fn show_todo(title: str) -> int:
        ...
    fn todo_count(handle: int) -> int:
        ...
    fn todo_get(handle: int, index: int) -> str:
        ...
    fn get_str(handle: int) -> str:
        ...
    fn release(handle: int) -> None:
        ...


class FormBridgeExports->FormBridgeExports:
    fn __init__(self: Self) -> None:
        ...
    fn release(handle: int) -> None:
        ...
    fn arrow_release(handle: int) -> None:
        ...
    fn free_str(ptr: int) -> None:
        ...
    fn message_box(title_ptr: int, title_len: int, msg_ptr: int, msg_len: int, buttons: int) -> int:
        ...
    fn input_box(title_ptr: int, title_len: int, prompt_ptr: int, prompt_len: int) -> int:
        ...
    fn show_note(title_ptr: int, title_len: int) -> int:
        ...
    fn get_note_title(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn get_note_content(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn show_todo(title_ptr: int, title_len: int) -> int:
        ...
    fn todo_count(handle: int) -> int:
        ...
    fn todo_get(handle: int, index: int, out_ptr: int, out_len: int) -> None:
        ...
    fn get_str(handle: int, out_ptr: int, out_len: int) -> None:
        ...

