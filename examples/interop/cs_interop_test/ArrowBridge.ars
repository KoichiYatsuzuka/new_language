class ArrowExports->ArrowExports:
    fn __init__(self: Self) -> None:
        ...
    fn Release(handle: int) -> None:
        ...
    fn Calculator_new_0() -> int:
        ...
    fn Calculator_new_1(initial: float) -> int:
        ...
    fn Calculator_Add(a: int, b: int) -> int:
        ...
    fn Calculator_Subtract(a: int, b: int) -> int:
        ...
    fn Calculator_Multiply(a: int, b: int) -> int:
        ...
    fn Calculator_Divide(a_bits: int, b_bits: int) -> int:
        ...
    fn Calculator_Power(base_bits: int, exp_bits: int) -> int:
        ...
    fn Calculator_Sqrt(x_bits: int) -> int:
        ...
    fn Calculator_getPi() -> int:
        ...
    fn Calculator_getE() -> int:
        ...
    fn Calculator_inst_Accumulate(handle: int, val_bits: int) -> None:
        ...
    fn Calculator_inst_GetAccumulated(handle: int) -> int:
        ...
    fn Calculator_inst_Reset(handle: int) -> None:
        ...
    fn TextProcessor_new(text: int, textLen: int) -> int:
        ...
    fn TextProcessor_inst_getText(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_inst_getLength(handle: int) -> int:
        ...
    fn TextProcessor_inst_ToUpper(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_inst_ToLower(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_inst_Trim(handle: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_inst_Contains(handle: int, sub: int, subLen: int) -> int:
        ...
    fn TextProcessor_inst_Replace(handle: int, old_ptr: int, old_len: int, new_ptr: int, new_len: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_inst_Append(handle: int, s_ptr: int, s_len: int) -> None:
        ...
    fn TextProcessor_Repeat(s_ptr: int, s_len: int, times: int, out_ptr: int, out_len: int) -> None:
        ...
    fn TextProcessor_IsNullOrEmpty(s_ptr: int, s_len: int) -> int:
        ...
    fn EventSource_StartTimer(signalId: int, count: int, intervalMs: int) -> None:
        ...
    fn EventSource_FireOnce(signalId: int, payload: int, payloadLen: int) -> None:
        ...
    fn FreeStr(ptr: int) -> None:
        ...


class Calculator->Calculator:
    fn __init__(self: Self) -> None:
        ...
    fn Add(a: int, b: int) -> int:
        ...
    fn Subtract(a: int, b: int) -> int:
        ...
    fn Multiply(a: int, b: int) -> int:
        ...
    fn Divide(a: float, b: float) -> float:
        ...
    fn Power(base_: float, exp: float) -> float:
        ...
    fn Sqrt(x: float) -> float:
        ...
    fn Accumulate(self: Self, value: float) -> None:
        ...
    fn GetAccumulated(self: Self) -> float:
        ...
    fn Reset(self: Self) -> None:
        ...
    fn getPi() -> float:
        ...
    fn getE() -> float:
        ...


class EventSource->EventSource:
    fn __init__(self: Self) -> None:
        ...
    fn StartTimer(signalId: int, count: int, intervalMs: int) -> None:
        ...
    fn FireOnce(signalId: int, payload: str) -> None:
        ...


class TextProcessor->TextProcessor:
    fn __init__(self: Self) -> None:
        ...
    fn getText(self: Self) -> str:
        ...
    fn getLength(self: Self) -> int:
        ...
    fn ToUpper(self: Self) -> str:
        ...
    fn ToLower(self: Self) -> str:
        ...
    fn Trim(self: Self) -> str:
        ...
    fn Contains(self: Self, sub: str) -> bool:
        ...
    fn Replace(self: Self, old: str, new_: str) -> str:
        ...
    fn Split(self: Self, sep: str) -> list[str]:
        ...
    fn Append(self: Self, s: str) -> None:
        ...
    fn Repeat(s: str, times: int) -> str:
        ...
    fn Join(sep: str, parts: list[str]) -> str:
        ...
    fn IsNullOrEmpty(s: str) -> bool:
        ...

